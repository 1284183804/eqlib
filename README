# eqlib

纯标准库 C++20 音频均衡器与动态处理库。零第三方依赖（仅 dr_libs 单头文件解码器），跨平台，同时产出静态库与动态库。

**当前版本：0.9.2**

[![CI](https://github.com/1284183804/eqlib/actions/workflows/ci.yml/badge.svg)](https://github.com/1284183804/eqlib/actions/workflows/ci.yml)

---

## 简介

eqlib 是一个用 C++20 编写的音频均衡器与动态处理库，提供 C API 与 C++ 封装两套接口。

设计边界：**库本身不做整体增益，只做相对频响修正。** 整体电平交由调用方通过 AGC 或手动增益决定。

核心能力：

- 1~93 段任意配置的动态多频段均衡
- 参考曲线注入（JSON → bin → 库）
- 内置 5 条目标曲线（Flat / Harman / Diffuse / Studio / Loudness）
- 音频文件直接解码（WAV / MP3 / FLAC）
- 任意比例重采样（多相 FIR，256 相位）
- 线性相位 / 混合相位 / 中侧 / 侧链 / 饱和 / AGC

---

## 特性

### 核心能力

| 特性 | 说明 |
|---|---|
| 参数 EQ | 固定 7 段，8 种滤波器类型 |
| 动态多频段 | 1~93 段，独立包络、增益、Attack / Release |
| 参考曲线注入 | 最多 20001 点，JSON → bin → 库 |
| 内置曲线 | Flat / Harman / Diffuse / Studio / Loudness |
| 动态 EQ | 1:∞ 压缩，每段独立目标 dBFS、动态范围 |
| 侧链 | 外部信号控制增益，已接入多频段动态 |
| 中侧 | Mid / Side 分别调增益 |
| 频谱分析 | radix-2 FFT，5 种窗函数，多通道独立 |
| 自动 EQ | 参考曲线 + 频谱匹配，输出 7 段增益 |
| 自动增益 | 目标 dBFS、最大增益 / 衰减、噪声门 |
| 模拟饱和 | 软削波 / 磁带 / 电子管 / 二极管 |
| 线性相位 | FIR 核，群延迟固定 |
| 混合相位 | 每段独立选择最小 / 线性 / 零相位 |
| 多通道多线程 | 常驻线程池 |
| 音频导入 | WAV / MP3 / FLAC 解码 |
| 音频导出 | WAV 文件 / 内存 |
| 重采样 | 任意采样率比例，多相 FIR |

### 工程特性

| 特性 | 说明 |
|---|---|
| 零依赖 | 核心只用 C++ 标准库 |
| C API + C++ 封装 | `eqlib.h` + `eq.hpp` |
| 静态库 + 动态库 | 一次编译同时产出 |
| 跨平台 | Windows / Linux / macOS |
| 编译防火墙 | 动态库只导出 C API 与 C++ 封装层 |
| 参数无锁队列 | SPSC（7 段 EQ 基础参数） |
| 多通道独立 | 每通道独立滤波 / 包络状态 |

---

## 目录结构

```text
eqlib/
├── dyjk/eqlib/
│   ├── eqlib.h            C API 唯一公开头
│   └── eq.hpp             C++ 封装层
├── nbym/
│   ├── per_channel.h      多通道容器（模板）
│   ├── wav_common.h/.cpp  WAV 公共解析
│   ├── gglx.h             公共类型
│   ├── yyh.h              可见性宏
│   ├── rz.h/.cpp          日志
│   ├── csdl.h             SPSC 参数队列
│   ├── mpmc.h             MPMC 队列
│   ├── hxhc.h/.cpp        环形缓冲
│   ├── sejlbq.h           双二阶滤波器
│   ├── jhqhx.h/.cpp       7 段参数 EQ
│   ├── dtjhq.h/.cpp       动态多频段
│   ├── neizhiqx.h/.cpp    内置曲线
│   ├── cl.h/.cpp          侧链
│   ├── zc.h/.cpp          中侧
│   ├── ppfx.h/.cpp        频谱分析
│   ├── zdjhq.h/.cpp       自动 EQ
│   ├── zdzy.h/.cpp        自动增益
│   ├── bh.h/.cpp          饱和
│   ├── dtd.h/.cpp         多通道多线程
│   ├── wjdx.h/.cpp        WAV 文件读写
│   ├── nccwj.h/.cpp       WAV 内存读写
│   ├── ypdr.h/.cpp        音频导入
│   ├── caiyang.h/.cpp     重采样
│   ├── xxwjhq.h/.cpp      线性相位 EQ
│   ├── hhxw.h/.cpp        混合相位 EQ
│   ├── eq.cpp             C++ 封装实现
│   ├── dr_impl.cpp        dr_libs 实现宏
│   ├── nbty.h             内部句柄定义
│   └── dwjk.cpp           C API 实现
├── dsf/                   第三方单头文件
├── cs/                    测试（23 个）
├── tools/
│   └── inject_curve.py    曲线注入工具
├── .github/workflows/
│   └── ci.yml             GitHub Actions
└── CMakeLists.txt
